# 中文书源筛选记录（2026-09-08）

## 已进入默认目录

- 漫画1234：2026-09-09 在 Lenovo TB331FC 上通过真实关键词搜索、详情、章节、阅读页与首图完整检测。图片 CDN 虽返回 `application/octet-stream`，文件头确认为 WebP。
- 包子漫画：2026-09-09 在 Lenovo TB331FC 上通过真实关键词搜索、详情、章节、阅读页与首图完整检测。搜索使用无跳转的 `cn.webmota.com` HTTPS 镜像；入口与阅读页的域名轮换已加入精确白名单，无法直连的 `bzcdn.net` 图片改走已验证的 `s2.baozimh.com` 同路径镜像。
- 可漫画：2026-09-09 在 Lenovo TB331FC 上通过真实关键词搜索、详情、章节与首张编号正文图片完整检测。正文页只读取公开的页数和图片目录变量，在客户端生成受 500 页上限与精确图片域名白名单保护的编号图片地址，不执行网页脚本。

> 复核说明：旧版健康探针在存在热门入口时会以热门列表代替搜索。该问题已修复，并新增回归测试，
> 所以本记录不再把旧版第一阶段结果表述为“真实搜索通过”。

## 暂不进入默认目录

- 咚漫：搜索、详情和章节可用，但阅读页返回连接错误，判为部分可用。
- 爱奇艺漫画：旧搜索入口已跳转，现有规则不能证明完整阅读链健康。
- 腾讯动漫：搜索可访问，但阅读图片依赖动态加密流程，声明式规则尚不支持。
- 再漫画、快看漫画：存在令牌、动态脚本或专用接口依赖，待逐个建立可维护适配。
- 妙趣漫画：目录页可访问，但真实关键词搜索当前返回 HTTP 404，且阅读页仍依赖固定密钥解码；未进入默认目录。
- zero搬运网：网页链结构较简单，但目前仅提供 HTTP 且公开扩展标记为混合内容；未进入面向普通用户的默认安全目录。
- 如漫画、妙趣漫画：阅读页依赖打包或加密脚本，未把任意脚本执行能力放进默认规则系统。
- Pica：需要登录、签名和密钥，不进入免登录默认目录。

本记录是当日技术检测结果，不代表第三方站点授权、永久可用或内容合规结论。后续只有完成实体设备四阶段复检的规则才可进入 `catalog.json`。
